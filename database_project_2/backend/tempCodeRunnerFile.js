    //   for (const d of Data2) {
    //     for(const dd of d){
    //         const startStation = dd.start_station.trim(); // Remove any leading or trailing spaces
    //     const endStation = dd.end_station.trim(); // Remove any leading or trailing spaces
        
    //     // Check if both start and end stations exist in the graph
    //     if (graph.has(startStation) && graph.has(endStation)) {
    //         // Add neighbors between start and end stations with a weight of 10
    //         graph.get(startStation).addNeighbor(endStation, 10);
    //         graph.get(endStation).addNeighbor(startStation, 10);
    //     }
    //     }
    // }